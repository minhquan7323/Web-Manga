version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "4001:4001"
    networks:
      - mynetwork

  frontend:
    build: ./frontend
    ports:
      - "4000:80"
    depends_on:
      - backend
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
